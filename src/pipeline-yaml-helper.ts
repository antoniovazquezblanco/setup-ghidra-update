import * as yaml from 'yaml';

async function getDocumentActions(doc: yaml.Document) {
    doc.contents...
}

export async function hasSetupGhidraAction(file: string) {
    const document = yaml.parseDocument(file);

}